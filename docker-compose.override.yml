services:
  frontend:
    volumes:
      - ./frontend:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf

  app:
    volumes:
      - .:/app
    environment:
      - FLASK_DEBUG=True
      - APP_ENV=development
